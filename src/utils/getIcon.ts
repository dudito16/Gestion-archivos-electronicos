import {
  Activity,
  Archive,
  ArrowRightLeft,
  AudioLines,
  BellRing,
  BookOpen,
  BookOpenCheck,
  ClipboardList,
  Database,
  DatabaseZap,
  FileCode,
  FilePlus,
  FileSearch,
  FileSignature,
  FileStack,
  FileText,
  FileType,
  FileX,
  FolderKanban,
  FolderOpen,
  FolderTree,
  FolderX,
  GitCompareArrows,
  HelpCircle,
  Image,
  Inbox,
  KeyRound,
  LayoutGrid,
  Link2,
  Mail,
  Radar,
  ReceiptText,
  Route,
  Scale,
  ScanLine,
  Sheet,
  ShieldCheck,
  Stamp,
  Tags,
  Target,
  Video,
  Workflow,
  type LucideIcon,
} from "lucide-react";

/**
 * Curated icon map for lesson/week data (which stores icon names as plain
 * strings). Explicit imports keep the bundle tree-shakeable — importing
 * lucide-react's full `icons` barrel would pull in every icon in the library.
 * Add an entry here whenever a new lesson icon name is introduced.
 */
const iconMap: Record<string, LucideIcon> = {
  Activity,
  Archive,
  ArrowRightLeft,
  AudioLines,
  BellRing,
  BookOpen,
  BookOpenCheck,
  ClipboardList,
  Database,
  DatabaseZap,
  FileCode,
  FilePlus,
  FileSearch,
  FileSignature,
  FileStack,
  FileText,
  FileType,
  FileX,
  FolderKanban,
  FolderOpen,
  FolderTree,
  FolderX,
  GitCompareArrows,
  HelpCircle,
  Image,
  Inbox,
  KeyRound,
  LayoutGrid,
  Link2,
  Mail,
  Radar,
  ReceiptText,
  Route,
  Scale,
  ScanLine,
  Sheet,
  ShieldCheck,
  Stamp,
  Tags,
  Target,
  Video,
  Workflow,
};

export function getIcon(name: string): LucideIcon {
  return iconMap[name] ?? HelpCircle;
}
